{
    "version": 2,
    "builds": [
        {
            "src": "/js/server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["json/db.json"]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}